import net.jini.jeri.JeriExporter;

org.wonderly.jini.reef {

  initialMemberGroups = new String[] { System.getProperty("user.name") + "InstallVerifyGroup" };

  persistenceDirectory="log/reef.log";

  serverExporter = new JeriExporter((ActivationID) $data, 0);

}
